# fire_project

Creates common folder structure for new projects for consistency.

## Usage

``` r
fire_project(
  quiet = FALSE,
  dir = c("dropbox", "onedrive", "custom"),
  drive = NULL,
  number = TRUE
)
```

## Examples

``` r
if (FALSE) { # \dontrun{
fire_project()
} # }
```
